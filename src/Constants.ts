import * as path from 'path';

export const minimaConfigFileFullPath = path.resolve('./minima.json');
export const defaultVersion = '0.0.0';
export const defaultPluginState = 'active';
export const defaultPluginsDirectory = 'plugins';
export const installedPluginState = 'installed';
export const defaultStartLevel = 50;
export const defaultFrameworkStartLevel = 100;
export const pluginConfigFileName = 'plugin.json';
export const defaultActivator = 'Activator.js';
